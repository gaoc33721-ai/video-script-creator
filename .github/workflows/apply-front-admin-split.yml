name: Apply frontend and admin split

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    name: Deprecated one-time hotfix
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo "This one-time frontend/admin split hotfix is intentionally disabled."
          echo "Use .github/workflows/deploy-ecs.yml for production deployments."
